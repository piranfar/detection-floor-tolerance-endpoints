# Archived drafts — not part of the live manuscript

Every file here is stale: from an earlier round of this paper's development
(a superseded title, a superseded target venue, a review round already
closed) and is not read by any build script (`src/build_*.py`,
`run_all.py`). Nothing here should be cited, edited, or treated as current
when reasoning about the manuscript's title, abstract, methods, results, or
review status.

| File | Why archived |
|---|---|
| `RESPONSE_TO_REVIEW.md`, `RESPONSE_TO_REVIEW_R2.md` | Both address a manuscript titled *"Score only the kill the assay can see"* — a title from before the current title ("Rifampicin tolerance classification is bounded by assay detection floor among clinical Mycobacterium tuberculosis isolates") existed. That review round is closed; nothing in either file describes the current text. |
| `SUPPLEMENTAL_MATERIAL.md` | Also carries the old title. Superseded by `manuscript/SUBMISSION_SUPPLEMENT.md`, which `src/build_submission.py` generates fresh from `MANUSCRIPT.md` on every rebuild. |
| `AAC_SUBMISSION_CHECKLIST_superseded.md` | AAC was the target venue before JMM (Journal of Microbiological Methods) was chosen — see `manuscript/JMM_SUBMISSION_CHECKLIST.md` for the current, live checklist. Already named `_superseded` by whoever wrote it; moved here rather than left loose in `manuscript/`. |
| `REFERENCES_DRAFT.md` | A pre-verification reference list compiled 2026-09-06, before the target journal was chosen and before house style was applied. Superseded by `manuscript/references.json` (source of truth) and `manuscript/references.md` (generated by `src/build_references.py`). |
| `FIX_PROMPTS_R3.md` | A round-3 fix-prompt scratch file at repo root, pinned to a specific old commit (`7722ac3`). Not referenced by any build script. |
| `P1.23_SIMULATION_OVERVIEW_FA.md` | A Persian-language simulation overview for a *different* numbered project item, unrelated to this paper's own pipeline. |
| `IDEA.md` | A two-word scratch note ("TB, Antibiotic"). No content worth keeping separately from the manuscript itself. |

If a future session needs to recover something from one of these (e.g. to
check what an earlier reviewer round actually said), read it here
explicitly — do not let it surface as if it described the live manuscript.
